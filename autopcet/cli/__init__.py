"""Command-line helpers for preparing the inputs of a PCET calculation.

These wrap :mod:`autopcet.structure` and :mod:`autopcet.gaussian` in argument
parsers and are installed as the ``autopcet-*`` console scripts. Reading
structure files in formats other than xyz needs ASE, which the ``scripts``
extra installs.
"""
